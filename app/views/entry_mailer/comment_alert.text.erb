A comment/remark has been created for:

	<%= @entry.vehicle %>
	(<%= @entry.plate_no %>)
	
	Chassis/Serial No. or VIN: 
		<%= @entry.serial_no %> 
	
	Engine/Motor No.: 
		<%= @entry.motor_no %>

Comment/remark:

	<%= @comment.comment %>
	
	
******** ******** *******

 
<% if @recipient && @recipient.has_role?('seller') %>
View it here:	http://www.ebid.com.ph<%= seller_show_path(:user_id => @recipient.username, :brand => @entry.car_brand.name.downcase, :id => @entry.id) %>
<% else %>
View it here:	http://www.ebid.com.ph<%= entry_path(@entry) %>
<% end %>

******** ******** *******
