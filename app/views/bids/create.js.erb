<% @line_items.each do |line_item| %>
	$("#amounts_<%= line_item.id %>").html("<%= escape_javascript(render 'seller/bid_set', :item => line_item) %>")
<% end %>
$("#bidding-message").text("Thanks for bidding!");
$("#bidding-message").effect("highlight", {}, 3000)
$("form")[0].reset();    

