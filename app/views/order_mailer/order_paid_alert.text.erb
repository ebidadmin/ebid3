Hi <%= @order.seller.profile.first_name %>!

<%= @entry.user.company_name %> tagged a Purchase Order as "Paid":

	Vehicle: 		<%= @order.entry.vehicle %>
	Plate No.: 	<%=h @order.entry.plate_no %>

	Parts Delivered: 
	
		<% @order.order_items.each do |li| %>
		[ <%= li.quantity %> ]  <%= li.line_item.part_name.html_safe %>
		<% end %>
		
	PO Released:     <%= @order.created_at.strftime('%b %d, %Y') %>	
	Date Delivered:  <%= @order.delivered.strftime('%b %d, %Y') %>

Please login to your account to confirm this payment (http://www.ebid.com.ph<%= seller_feedback_path(:user_id => @order.seller, :search => @entry.plate_no) %>).

In case you cannot confirm this action within three (3) days, it means that you have already received the payment 
and E-Bid shall consider this PO as "Closed".

Thank you!
