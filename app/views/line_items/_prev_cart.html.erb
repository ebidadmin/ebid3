<h3>Saved Selection</h3>

<ul>
	<%= render 'shared/part_titles' %>
	<% for item in  @entry.line_items %>
		<li id="item_<%= item.id %>" class="selection-list">
			<%= render 'line_items/prev_item', :item => item %>
		</li>
	<% end %>
</ul>
