<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? "E-Bid Inc. | " + yield(:title) : "E-Bid Inc. - The Philippines' Vehicle Parts Auction Hub" %>
    </title>
		<!--[if !IE 6]><!-->  <%= stylesheet_link_tag 'application', :media => "all" %>      <!-- <![endif]-->
		<!--[if IE 6]><%= stylesheet_link_tag 'ie', :media => "screen" %><![endif]-->
    <%= csrf_meta_tag %>
		<link rel="shortcut icon" href="/images/base/favicon.ico" />
		<%= yield(:head) %>
  </head>

  <body>
		<div id="top">		
		<div class="container">
  		<%= render 'layouts/header' %>

      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "flash" %>
      <% end %>

 			<div id="content-main">
	      <%= content_tag :h1, yield(:title), :class => 'main-title' if show_title? %><!-- <br class='clear-both'> -->
	      <%= yield %>
			</div>
    </div>
		</div>    
		<div id="footer">
			<%= render 'layouts/footer' %>
		</div>
		<div class="spinner" style="display:none">
			<img src="/images/progress_large.gif" width="32" height="32" alt="" />
		</div>
		<div id="javascripts">
	    <%#= javascript_include_tag :defaults, 'rails.validations' %>
	    <%= javascript_include_tag :defaults %>
			<%= yield(:javascripts) %>
		</div>
  </body>
</html>
