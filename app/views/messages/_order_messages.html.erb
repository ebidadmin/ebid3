<% unless @priv_messages.blank? %>
	<%= content_tag :h4, pluralize(@priv_messages.count, 'message'), :class => 'instruction' %>
	<ul>
		<%= nested_messages @priv_messages.arrange(:order => :created_at) %>
	</ul>
<% else %>
	<%= content_tag :h4, 'No messages yet.', :class => 'instruction' %>
<% end %>
